 <tr>
      <td><%= user.email %>
  </td>
      <td><%= user.created_at %></td>
  <td>
    <%= link_to "Edit", edit_admin_user_path(user), :class => "btn btn-primary btn-xs"%>
    <% unless current_user == user%>
    <%= link_to "Delete", admin_user_path(user), :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => "btn btn-primary btn-xs" %>
    <%end%>
  </td>
</tr>