<% if resource.errors.any? -%>
  $("#change-password-form").html("<%= escape_javascript(render partial: 'form', locals: { resource: resource })%>");
<% else -%>
  $(window.location.replace("/"))
<% end -%>