<div class="ura-main">
  <div class="ura-left">
    <div class="ura-left-head">
      <div class="ura-left-head-box1"><input name="" type="checkbox" value="" /></div>
      <div class="ura-left-head-box2">Users</div>
      <div class="ura-left-head-box3">Roles</div>
    </div>
    <div class="ura-left-cnt">
      <table width="648" border="0" cellspacing="0" cellpadding="0" class="ura-left-cnt-tbl">
        <% @users.each do |u|%>
          <tr>
            <td align="center">
              <%= check_box_tag "users[#{u.id}]", u.id %>
            </td>
            <td><%= u.email %></td>
            <% u.roles.each do |role|%>
              <td class="role">
                <label><%= role.name %><span><img src="/assets/close-icon.png" width="14" height="14" alt="delete" /></span></label>
              </td>
            <%end%>
            <td><%= hidden_field 'application','id',value: @application.id %></td>
          </tr>
        <%end%>
      </table>
    </div>
  </div>
  <div class="ura-right ">
    <div class="ura-right-head">
      Roles
    </div>
    <div class="ura-right-cnt">
      
      <table>
        <% @roles.each do |role|%>
          <tr>
            <td>
              <label>
                <span>
                  <%= check_box_tag "roles[#{role.id}]", role.id %>
                </span>
                <%= role.name%>
              </label>
            </td>
          </tr>
        <%end%>
        <tr>
          <td>
            <button type="button" onclick="assign_roles()">Assign</button>
          </td>
        </tr>
      </table>
    </div>
  </div>
</div>
<script>
  function assign_roles(){
    var applicationId = $("#application_id").val()
    var n = $( "input:checked" ).length;
    var idSelector = function() { return this.id; };
    var checked = $(":checkbox:checked").map(idSelector).get();
    $.ajax('/applications/' + applicationId + '/assign_role_user', {
      type: 'POST',
      data: {user_role_map: [checked]},
      datatype: 'json',
      success: function(data, textStatus, jqHXR) {
        alert("Success")
      }
    });
  }
</script>